rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: This is temporarily open for debugging.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default rule: allow read/write access to authenticated users.
    // This is a permissive rule for development.
    // For production, you should implement more granular security rules.
    // Force redeploy: 2024-08-01 15:00:00 UTC
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
